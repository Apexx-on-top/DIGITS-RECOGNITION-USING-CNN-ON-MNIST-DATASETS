# DIGITS-RECOGNITION-USING-CNN-ON-MNIST-DATASETS
CNN-based handwritten digit recognition using the MNIST dataset. The model learns spatial features through convolution and pooling layers to classify digits (0–9) with high accuracy. Built with Python and TensorFlow/Keras, this project demonstrates a complete deep learning workflow.
